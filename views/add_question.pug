extends base
block content
    .card
        .card-body
            form(method='post' action='/question/add')
                .form-group
                    label(for='label-field')
                        small.form-text.text-muted  Titre de la question
                    input.form-control(name='tile' id='label-field')
                .form-group
                    label(for='description-field')
                        small.form-text.text-muted Description
                    textarea.form-control(id='description-field'  name='description')
                button(type='submit').btn.btn-block.btn-primary Ajouter